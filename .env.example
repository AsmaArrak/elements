BOT_TOKEN=your_discord_bot_token_here
# Optional: restrict slash command sync to one guild for instant updates during development
# Leave blank for global commands (takes ~1 hour to propagate)
GUILD_ID=
# Comma-separated Discord user IDs that can use /admin commands (right-click your name → Copy User ID)
ADMIN_IDS=
